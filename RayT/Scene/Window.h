#ifndef _WINDOW_
#define _WINDOW_


// Currently, can be just a static class. Consisting of some window params
class Window {
	public:
			static int mWindowWidth;
			static int mWindowHeight;
			//static unsigned char colorBuffer[500][500][3];
};

#endif
